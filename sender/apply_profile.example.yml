# Данные для автозаполнения форм отклика на сайтах компаний.
#
#   cp sender/apply_profile.example.yml sender/apply_profile.yml
#
# Настоящий файл (apply_profile.yml) в .gitignore — в репозиторий не попадёт.
# Пока его нет, автоотклик работает с пустым профилем: любая открывшаяся форма
# уходит в 'manual' с пометкой «не заполнены обязательные поля». Если автоотклик
# не нужен вовсе — EXTERNAL_APPLY_ENABLED=false в .env, и этот файл не понадобится.
#
# Незаполненное поле = пустая строка. Обязательный минимум, чтобы формы вообще
# начали заполняться: full_name (или first_name + last_name) и email.

full_name: "Иван Иванов"
first_name: "Ivan"          # латиницей — большинство ATS не принимает кириллицу
last_name: "Ivanov"
email: "ivan@example.com"
phone: "+7 777 000 00 00"

city: "Алматы"
country: "Казахстан"

# linkedin и telegram перебиваются строками из sender/signature.txt, если они
# там заполнены: контакт в форме отклика обязан совпадать с контактом в подписи
# письма. Меняй его в signature.txt, здесь он нужен только как запасной.
linkedin: "https://www.linkedin.com/in/ivanov"
telegram: "@ivanov"
github: "https://github.com/ivanov"
portfolio: ""

# Как отвечать на «Are you authorized to work in ...?»
work_authorization: "Гражданин Казахстана, разрешение на работу в РК не требуется"
needs_visa_sponsorship: false
open_to_relocation: true

desired_salary: ""
# Текущая зарплата — ПУСТО по умолчанию, и это не забывчивость.
# «Current CTC» / «What is your current salary?» у части работодателей (чаще
# всего индийский рынок) обязателен и принимает ТОЛЬКО число: фразу вроде
# «не указываю» LinkedIn отвергает «Недопустимым значением», и заявка встаёт.
# Но ожидаемую зарплату модель считает по вакансии, а текущая — факт о тебе,
# которого у неё нет; выдуманное число уехало бы работодателю как твоё.
# Впиши сюда число (например «2400000» — годовой доход в тенге), если хочешь,
# чтобы такие заявки уходили. Пока пусто — они честно достаются тебе в ручные.
current_salary: ""
          # пусто — вопрос уйдёт модели
# Сколько лет опыта называть в ответ на «years of experience».
# 0 = не подставлять, оставить вопрос модели.
min_experience_years: 3
notice_period: "2 недели"

# Готовые ответы на повторяющиеся вопросы форм.
# Ключ — кусок текста вопроса в нижнем регистре; сравнение по вхождению подстроки.
# Пустое значение означает «пусть отвечает модель».
custom_answers:
  "how did you hear about us": "LinkedIn"
  "telegram handle": "@ivanov"
  # НЕ задавай здесь «years of experience»: custom_answers проверяются РАНЬШЕ
  # правила про опыт, так что ключ отсюда отменит min_experience_years и в
  # выпадающем списке диапазонов ответить будет нечем.
  "why do you want to work": ""
